<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="App3.Favorite"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:lab="using:CommunityToolkit.Labs.WinUI"
    xmlns:tool="using:CommunityToolkit.WinUI.Controls"
    Background="{ThemeResource DesktopAcrylicTransparentBrush}">

    <Grid Padding="15,0,15,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="12*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid  Grid.Row="0" Margin="10,10,0,10" VerticalAlignment="Center" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="80"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="收藏集" FontSize="20" FontWeight="Bold" x:Name="de"  Foreground="LightSeaGreen" VerticalAlignment="Center" HorizontalAlignment="Left" FontFamily="Noto Sans SC"></TextBlock>
            <ComboBox x:Name="FavoriteBox" Grid.Column="1" HorizontalAlignment="Right" Margin="0,0,20,0" PlaceholderText="切换收藏夹" Background="Transparent" ItemsSource="{x:Bind Favos}" DisplayMemberPath="GroupName" SelectionChanged="FavoriteBox_SelectionChanged" SelectedItem="{x:Bind SelectedFavo,Mode=TwoWay}" Height="35.5">
                
            </ComboBox>
            <Button Grid.Column="2"  HorizontalAlignment="Right" x:Name="ChangeSort" Click="ChangeSort_Click" Background="Transparent">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <ic:SymbolIcon Symbol="StarAdd" FontSize="16"  x:Name="SortIcon"></ic:SymbolIcon>
                    <TextBlock Text="排序" x:Name="SortIndicator"></TextBlock>
                </StackPanel>
            </Button>
        </Grid>
        
        <ScrollViewer Grid.Row="1"  Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="7" Margin="0,0,0,15" >
            <ItemsRepeater ItemsSource="{x:Bind tiles}" x:Name="Collection" Loaded="Collection_Loaded">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <HyperlinkButton HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch" CornerRadius="0" x:Name="Content" Click="Content_Click" BorderThickness="0.5" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
                            <Grid Padding="3,0,3,0" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="50"></ColumnDefinition>
                                    <ColumnDefinition Width="200"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding sort}" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" HorizontalTextAlignment="Center"></TextBlock>
                                <TextBlock Text="{Binding section}" Grid.Column="3" Grid.Row="0"  VerticalAlignment="Top"></TextBlock>
                                <TextBlock Text="{Binding title}" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontFamily="Noto Sans SC"></TextBlock>
                                <TextBlock Text="{Binding time}" Grid.Column="3" Grid.Row="1"  VerticalAlignment="Bottom" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                            </Grid>
                        </HyperlinkButton>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

        <local:InfoFlower Grid.Row="0" Grid.RowSpan="2" x:Name="Flower"></local:InfoFlower>
    </Grid>
</Page>
