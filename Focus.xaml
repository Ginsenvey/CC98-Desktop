<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CC98.Focus"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CC98"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:lab="using:CommunityToolkit.Labs.WinUI"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    
    Background="{ThemeResource DesktopAcrylicTransparentBrush}">

    <Grid Padding="15,5,15,15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="60"></RowDefinition>
            <RowDefinition Height="12*"></RowDefinition>
        </Grid.RowDefinitions>
        <SelectorBar VerticalAlignment="Center" SelectionChanged="TypeChoice_SelectionChanged" x:Name="TypeChoice">
            <SelectorBarItem Text="好友动态" IsSelected="True" Tag="0"> </SelectorBarItem>
            <SelectorBarItem Text="收藏更新" Tag="1"></SelectorBarItem>
        </SelectorBar>

        <ScrollViewer Grid.Row="1"   BorderThickness="1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" CornerRadius="5" >
            <ItemsRepeater x:Name="STileList" Loaded="STileList_Loaded">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:StandardPost">
                        <HyperlinkButton HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch" CornerRadius="0" Click="Tile_Click" DataContext="{x:Bind}">
                            <Grid Padding="0,10,0,10" ColumnSpacing="5" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30"></ColumnDefinition>
                                    <ColumnDefinition Width="4*"></ColumnDefinition>
                                    <ColumnDefinition Width="*"></ColumnDefinition>
                                    <ColumnDefinition Width="40"></ColumnDefinition>
                                    <ColumnDefinition Width="70"></ColumnDefinition>
                                </Grid.ColumnDefinitions>
                                <ic:SymbolIcon Symbol="LeafOne"></ic:SymbolIcon>
                                <TextBlock Text="{x:Bind title}" Grid.Column="1" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                                <TextBlock Text="{x:Bind author}" Grid.Column="2" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                                <Border Grid.Column="3" Background="Transparent"  HorizontalAlignment="Left" >
                                    <StackPanel Orientation="Horizontal" Spacing="5" >
                                        <ic:SymbolIcon Symbol="Chat" FontSize="11"></ic:SymbolIcon>
                                        <TextBlock Text="{x:Bind reply}" Grid.Row="2"  HorizontalAlignment="Right" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                                    </StackPanel>
                                </Border>

                                <Border Grid.Column="4" Background="Transparent"  HorizontalAlignment="Left" >
                                    <StackPanel Orientation="Horizontal" Spacing="5" >
                                        <ic:SymbolIcon Symbol="Fire" FontSize="11"></ic:SymbolIcon>
                                        <TextBlock Text="{x:Bind hit}"   HorizontalAlignment="Right" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"></TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </HyperlinkButton>

                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>








    </Grid>
</Page>
