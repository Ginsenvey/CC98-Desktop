# CC98 页面性能与内存优化待办

> 来源：9 个子代理并行审计 21 个页面 + 共享组件（共 107 条发现），高优先级项已人工核实源码。
> 更新规则：每解决一项即勾选；每批修改完成后构建验证。

## 状态图例
- `[ ]` 待办
- `🔄` 进行中
- `[x]` 已完成
- `⏸` 等待用户决策
- `🚫` 不做（用户排除/后续重构）

---

## A. 崩溃风险（P0，全部优先解决）

- [x] A1 `OpenIdLoginPage`：登录按钮 async void 无 try/catch（网络失败崩溃）、失败后按钮不恢复 ✅已构建
- [x] A2 `SectionPage` + `BoardSectionManager`：无缓存+接口失败时异常链崩溃 ✅已构建
- [x] A3 `TopicPage`/`ChatPage`/`DiscoverPage`/`FocusPage`：`First()` → `FirstOrDefault()` ✅已构建
- [x] A4 `MainWindow` VPN：`ReloginVpn`/`VpnConfirmAsync` 无 try/catch（VpnService 明确 throw）✅已构建
- [x] A5 `IndexPage`：修改 MemoryCache 共享缓存对象 → 二次进入 Url 双前缀崩溃 ✅已构建
- [x] A6 `TopicPage`：翻页/跳页并发 `LoadReply` → 楼层重复/串页 ✅已构建
- [x] A7 `IndexPage`：`LoadFromCacheAsync` 无限递归 ✅已构建
- [x] A8 `MainWindow` `PinOff_Click`：`First()` 崩溃 ✅已构建
- [x] A9 `SketchPage`：对话框/发送 async void 无 try/catch（含 SendButton 永久禁用）✅已构建

## B. 内存

- [x] B1 设置页主题缩略图/预览：降低解码分辨率（`DecodePixelWidth≈120`）✅已构建
- 🚫 B2 正文/回复图片：保持原始分辨率（用户决定不改）
- 🚫 B3 主页/个人头像：保持原始分辨率（用户决定不改）
- [x] B4 `SmartImageLoader`：静态弱引用缓存清理死条目 ✅已构建
- [x] B5 `DiscoverPage`：`RandomTopics` 无界增长 + `SizeChanged` 退订后不恢复 ✅已构建
- [x] B6 无虚拟化列表改虚拟化布局 —— **已确认 ItemsRepeater 默认(StackLayout)即虚拟化,无需改布局类**(winmd 中无 VirtualizingStackLayout/ItemsStackLayout,WinUI3 已合并进 StackLayout)
- ⏸ B6a/b `ProfilePage`/`BoardPage`：`ScrollViewer>StackPanel>ItemsRepeater` 中 **StackPanel 给 ItemsRepeater 无限测量高度,虚拟化失效**（与布局类无关）——**用户决定：保持现状**（列表量不大，重构有回归风险）
- 🚫 B7 `SearchPage`：翻页 AddRange 无界增长（用户决定后续重构，不管）

## C. 性能

- [x] C1 `TopicPage`：`LoadTopicInfo`+`LoadReply` 及内部请求并行化 ✅已构建
- [x] C2 `ProfilePage`：`LoadUserProfile`/`LoadRecentTopic` 并行化 ✅已构建
- [x] C3 `BoardPage`：`GetData`/`LoadTopics` 并行化 ✅已构建
- [x] C4 `DiscoverPage`：`GetNewTopic`/`GetRandomTile` 并行化 ✅已构建
- [x] C5 `GamePage`：`RefreshStat` 两个请求并行化 ✅已构建
- [x] C6 `MainWindow`：定时器 `FetchIndex`/`RefreshMessage` 并行化 ✅已构建
- [x] C7 `Objects/Base.cs` `Increment.LoadMore`：加 in-flight 防重入守卫 ✅已构建
- [x] C8 `ChatPage`：循环 `Insert(0)` → 批量填充(消除 O(n²) 移动) ✅已构建
- [x] C9 `SketchPage`：表情类型悬停重载 → 缓存 + 类型守卫 ✅已构建
- [x] C10 `SketchPage`：`File.ReadAllBytes` → `StreamContent` 流式上传 ✅已构建
- [x] C11 `SketchPage`：编辑器每次击键全量重渲染 → 300ms 防抖 ✅已构建
- [x] C12 `GamePage`：`StartDraw` 防重入（真实扣费请求）✅已构建
- [x] C13 `MainWindow`：`AnimateButton` 每次 new Storyboard → 字段复用 ✅已构建

## D. 正确性/逻辑

- [x] D1 `MessagePage`：`NoticeCard_Click` 已删除帖子缺 `return` ✅已构建
- [x] D2 `GamePage`：连抽概率列表误用 `GachaInfo1` → `GachaInfo2` ✅已构建
- [x] D3 `GamePage`：`card.ImageUri.Substring(1,...)` 空串崩溃 ✅已构建
- [x] D4 `FollowPage`：`HasMore` 判定与全库约定不一致（关注列表只显示第一页）✅已构建
- [x] D5 `FavoritePage`：`HasMore` off-by-one ✅已构建
- [x] D6 `ChatPage`：`UserIncrement.HasMore` 误用 `ChatHistoryIncrement.PageSize` ✅已构建
- 🚫 D7 `TopicPage`：`MarkdownTextBlock_LinkClicked` url 恒为空 —— **所用 Labs 版 MarkdownTextBlock 不支持链接点击事件(Labs issue #584),无法绑定;已删除死代码,完整修复需升级/更换控件**
- [x] D8 `Picture`/`SmartImage`：Src 快速变化加载竞态（代次校验）✅已构建
- [x] D9 `Picture`：加载失败 shimmer 永不停止 ✅已构建
- [x] D10 `MediaViewer`：`Activated` 每次重置缩放/旋转 → 首次加载标志 ✅已构建
- [x] D11 `MediaViewer` 视频预览 —— **按用户决定删除视频预览功能**：MediaViewer 仅保留图片预览(移除 MediaPlayerElement 与视频分支),TopicPage 视频链接改走下载 ✅已构建
- [x] D12 `SketchPage`：文档按钮 upload 空标签 —— **按用户决定实现真正的文档上传**(GetSuffixs 增加文档后缀,DocumentsLibrary 选择器,上传后插入 [upload]URL[/upload]) ✅已构建
- [x] D13 `SketchPage`：上传成功判定 `url.Contains("file")` 脆弱 ✅已构建
- [x] D14 `BoardPage`：`TypeSelectorBar_SelectionChanged` 重复加载/重入 ✅已构建
- [x] D15 `MainWindow`：`LoadSettings` 死代码（`files[0]` 崩溃）+ `IsVpnEnabled=false` 副作用 ✅已构建
- [x] D16 `TopicPage`：`Like_Click` 防重入、`InitializeVote` 重复订阅/索引越界、`SmallProfile_Loaded` 兜底 ✅已构建
- [x] D17 `ChatPage`：`Send_Click` try/finally 恢复按钮 ✅已构建

---

## 备注
- `B6` 需确认各页面 XAML 结构（`ScrollViewer>StackPanel>ItemsRepeater` 中 StackPanel 会破坏虚拟化，需调整结构）。
- `D11`/`D12` 等待用户决策后更新。
