<?xml version="1.0" encoding="utf-8"?>

<Window
    x:Class="CC98.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:dev="using:DevWinUI"
    xmlns:tool="using:CommunityToolkit.WinUI.Controls"
    xmlns:infoFlower="using:CC98.Controls.InfoFlower"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid x:Name="RootGrid" UseLayoutRounding="True" Loaded="RootGrid_Loaded">
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Splash/23year.png" Stretch="UniformToFill" AlignmentX="Center" AlignmentY="Center" />
        </Grid.Background>
      
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="68" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"  Height="32" x:Name="GridTitleBar" />
        <Frame Grid.Row="1" Width="360" Padding="20" Background="Transparent" BorderThickness="0" x:Name="LoginFrame" />
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1" Height="1"
                VerticalAlignment="Top" />
        <TextBlock Grid.Row="2"  Text="版本: v1.2.6 release."
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"
                   Margin="20,0,0,0" />
        <StackPanel Orientation="Horizontal"  Grid.Row="2" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center" Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                    Margin="0,0,20,0" CornerRadius="6">
            <HyperlinkButton Background="Transparent" Padding="0" x:Name="Github"
                             NavigateUri="https://github.com/Ginsenvey/CC98-Desktop" ToolTipService.ToolTip="查看仓库"
                             Height="42" Width="42">
                <ImageIcon Source="/Assets/github.png" Width="25" Height="25" />
            </HyperlinkButton>
            <AppBarSeparator  BorderThickness="4"/>
            <HyperlinkButton Width="42" Height="42">
                <ic:SymbolIcon Symbol="Document" IconVariant="Color" />
            </HyperlinkButton>
            <AppBarSeparator  BorderThickness="4" />
            <HyperlinkButton Width="42" Height="42" Padding="0" NavigateUri="https://www.cc98.org">
                <ic:SymbolIcon Symbol="Home" IconVariant="Color" />
            </HyperlinkButton>
        </StackPanel>
        <infoFlower:InfoFlower x:Name="Flower"  Grid.RowSpan="3" Grid.Column="0" Grid.ColumnSpan="3"
                               HorizontalAlignment="Center" VerticalAlignment="Bottom" />
    </Grid>
</Window>