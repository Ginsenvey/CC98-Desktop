<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CC98.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CC98.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ic="using:FluentIcons.WinUI"
    xmlns:obj="using:CC98.Objects"
    xmlns:infoFlower="using:CC98.Controls.InfoFlower"
    Background="{ThemeResource DesktopAcrylicTransparentBrush}"
    mc:Ignorable="d">

    <Grid Padding="15,0,15,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="10,10,10,10" VerticalAlignment="Center">
            <TextBlock Text="足迹" FontSize="20" FontWeight="Bold"  Foreground="LightSeaGreen" VerticalAlignment="Center" HorizontalAlignment="Left" ToolTipService.ToolTip="此页面请求限速，可能卡顿"/>
            <Button x:Name="EnableRecordButton"  HorizontalAlignment="Right"  Background="Transparent" Visibility="Collapsed" Click="EnableRecordButton_Click"></Button> 
        </Grid>

        <ScrollViewer Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource AcrylicCardBrush}" BorderThickness="2" CornerRadius="7"
              Margin="0,0,0,15">
            <ItemsRepeater ItemsSource="{x:Bind HistoryTopics}" x:Name="HistoryTopicRepeater"
                   ElementPrepared="HistoryTopicRepeater_ElementPrepared">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="obj:SimpleTopicInfo">
                        <HyperlinkButton HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch"
                                 CornerRadius="0" x:Name="Content" Click="Content_Click" BorderThickness="0,1,0,1"
                                 BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                 Tag="{x:Bind Id}">
                            <Grid Padding="3,0,3,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                    <ColumnDefinition Width="200" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <ic:SymbolIcon Symbol="ChevronRight" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" FontSize="14"  Foreground="{ThemeResource AccentAAFillColorDefaultBrush}" />
                                <TextBlock Text="{x:Bind BoardName}" Grid.Column="3" Grid.Row="0" VerticalAlignment="Top" FontSize="12"/>
                                <TextBlock Text="{x:Bind Title}" Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{x:Bind LastBrowsingTime}" Grid.Column="3" Grid.Row="1" FontSize="12" VerticalAlignment="Bottom" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </Grid>
                        </HyperlinkButton>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

        <infoFlower:InfoFlower Grid.Row="0" Grid.RowSpan="2" x:Name="Flower" />
    </Grid>
</Page>
